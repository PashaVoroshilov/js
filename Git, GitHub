git config --global user.name "Pavel Voroshilov" // Фамилия, Имя кто вводит измененния в ветки;
git config --global user.email "Pavel Voroshilov"
git config --global core.editor vi,nano,emacs //изменение редактора git'a;
3 группы git:
Head 
Working Directory (песочница)
index

														Команды 
git init //создание в корне проекта папки git, которая будет следить за изменением проекта;
git status // посмотреть за изменениями в папке (файлах);
git add . //команда для добавления слежки за файлами;
git add -A (all) //добавление изменений в коммит;
														
														Коммиты
git commit //создания коммита (сохранение проекта);
git commit -m 'название нового коммита' //быстрое создание нового коммита;
git log //просмотр созданных коммитов;
git diff идентификатор коммита // просмотр конкретных изменений в коммите;
git reset --soft //откат к коммиту;
git reset --hard //откат с удалением других коммитов ДО;
git reset HEAD index.html // добавление файла в Working Directory из Index (изменение одного файла);
git cheackout -- index.html //откат файла до текущего коммита
														
														Работа с ветками
git checkout -b имя новой ветки //создание новой ветки и переход в нее;
git checkout имя ветки // переход в ветку на проекте;
git branch //просмотр веток;
git branch -d имя удаляемой ветки //удаление ветки;
git branch -v // просмотр изменений в ветках;
git merge имя ветки с которой соединить // перед соединением нужно перейти в папку с которой хотим соеденить;

git log --oneline //вывести краткую информацию коммитов и веток;
git log --oneline --graph //вывести краткую информацию коммитов и веток с графиком;

														Сохранение куска кода, как на флешку
git stash // место хранения кода;
git stash list //просмотр стэшей;
git stast apply stash@{0} //добавление стэша в коммит;
git stash drop stash@{0} //удаление стэша;
git stash pop //добавление последнего стэша и удаление его из листа;

														Работа с GitHub
git remote add origin https:адресс // добавить репозиторий;
git push -u origin master // отправка файлов на гитхаб;
git clone адресс, название папки-проекта // копирование репозитория с гитхаба;
git url set-url origin ссылка нового адресса репозитория // перезалив на другую ссылку;

														github-pages (удаленный репозиторий для фронтенда)


